# erp_new 前端待办清单

> 基于 Vue 3 + Quasar 的全栈微服务管理平台前端

## 1. 项目现状

### 1.1 应用结构

| 应用 | 框架 | 页面数 | 说明 |
|---|---|---|---|
| admin | Vue3 + Quasar | 56 | 平台运营（租户/计费/目录/票据/系统） |
| tenant | Vue3 + Quasar | 86 | 单位业务 |
| ops | Vue3 + Quasar | 19 | 运营面板 |
| social | Vue3 + Quasar | 13 | 社交端 |

### 1.2 共享层（packages/）

| 包 | 说明 |
|---|---|
| api | API 封装（alova） |
| boot | 启动引导（alova 实例 + 认证/租户注入） |
| capabilities | 能力开关 |
| components | 共享组件（138 vue / 33 ts） |
| composables | 组合式函数（57 文件） |
| i18n | 国际化（en-US / zh-CN） |
| pages | 共享页面（112 vue，待清理） |
| stores | 共享状态 |
| types | 类型定义（24 文件） |
| utils | 工具函数 |

## 2. 已完成 ✅

- [x] 清理 `node_modules/`、`dist/`、`.turbo/` 缓存
- [x] 更新 `.gitignore`（.turbo/、.quasar/、.kilo/、.vscode/）
- [x] 添加 `deploy/docker-compose.yml`

## 3. 待办事项

### 3.1 高优先级

- [ ] **packages/pages 清理**：112 个 vue 文件但 index.ts 是占位符，需清理未使用文件
- [ ] **packages/shared 占位**：检查 packages 中是否有仅导出字符串的 placeholder
- [ ] **类型安全**：清理 TypeScript `any` 类型（当前约 50 处）
- [ ] **路由治理**：清理未在路由中注册的页面文件
- [ ] **admin/tenant 去重**：两端页面高度重复，参考 sesms 三端分工模式重构

### 3.2 中优先级

- [ ] **薄封装页面清理**：9 个 7 行模板页面可改为路由直引共享包
- [ ] **API 层统一**：将 admin 内联 API 移到共享 api 包
- [ ] **依赖升级**：Quasar / Vite / Vue 版本对齐

### 3.3 低优先级

- [ ] **E2E 测试**：Playwright / Cypress
- [ ] **Lighthouse 审计**：性能、可访问性
- [ ] **Bundle 分析**：优化首屏加载

## 4. 构建脚本

```bash
# 开发
cd Frontend
pnpm --filter myai-admin dev

# 构建
pnpm --filter myai-admin build

# 类型检查
cd Frontend/apps/admin
npx vue-tsc --noEmit
```

## 5. 代码统计

| 维度 | 数量 |
|---|---|
| Vue 文件 | 575 |
| TypeScript 文件 | 549 |
| SCSS 文件 | ~20 |
| 包共享 | 10 个 packages |
| 应用 | 4 个 |
| 总文件 | ~1870 |
