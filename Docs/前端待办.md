# erp_new 前端待办清单

> 基于 Vue 3 + Quasar 的全栈微服务管理平台前端

## 1. 项目现状

### 1.1 应用结构

| 应用   | 框架          | 页面数 | 说明                                 |
| ------ | ------------- | ------ | ------------------------------------ |
| admin  | Vue3 + Quasar | 47     | 平台运营（租户/计费/目录/票据/系统） |
| tenant | Vue3 + Quasar | -      | 单位业务（路由引用 packages/pages）  |
| ops    | Vue3 + Quasar | -      | 运营面板（路由引用 packages/pages）  |
| social | Vue3 + Quasar | -      | 社交端（路由引用 packages/pages）    |

> **说明**: tenant/ops/social 的所有业务页面均通过 `packages/pages` 共享层引用，不再维护本地 `pages/` 目录。

### 1.2 共享层（packages/）

| 包           | 文件数 | 说明                                   |
| ------------ | ------ | -------------------------------------- |
| api          | -      | API 封装（alova）                      |
| boot         | -      | 启动引导（alova 实例 + 认证/租户注入） |
| capabilities | -      | 能力开关                               |
| components   | 138    | 共享组件                               |
| composables  | -      | 组合式函数                             |
| i18n         | -      | 国际化                                 |
| pages        | 111    | 共享页面（111 Vue，按业务域分目录）    |
| stores       | -      | 共享状态                               |
| types        | -      | 类型定义                               |
| utils        | -      | 工具函数                               |

## 2. 已完成 ✅

- [x] 清理 `node_modules/`、`dist/`、`.turbo/`、`.quasar/`、`.kilo/` 缓存
- [x] 更新 `.gitignore`（.turbo/、.quasar/、.kilo/、.vscode/）
- [x] 添加 `deploy/docker-compose.yml`
- [x] 重构 `packages/pages/src/index.ts`（112 真实导出）
- [x] 删除 `apps/tenant/src/pages/`（92 未引用文件）
- [x] 删除 `apps/ops/src/pages/`（19 未引用文件）
- [x] 删除 `apps/social/src/pages/`（13 未引用文件）
- [x] 删除 admin 9 个薄封装页面 + 清理路由
- [x] 清理 TypeScript `any` 类型（11 → 0）
- [x] 删除 10 个 tenant 死组件
- [x] 添加 admin not-found 路由
- [x] 租户组件去重（122 → 保留 ≤5 个特有组件）
- [x] 修复 security_headers.rs unsafe-inline 仅 dev 生效
- [x] 移除硬编码密码（Password@123）和邮件密码（"pass"）
- [x] 统一 package manager = pnpm 11+
- [x] pnpm workspace + turbo 配置
- [x] admin/tenant/ops/social 四端共享 packages/pages
- [x] **v0.8.0 前端类型安全加固**:
  - [x] 清理 TypeScript `any` 类型（20 → 0）
  - [x] 替换 `as unknown as` 双重断言（54 → 35，引入 unwrapData/unwrapPaginated 工具）
  - [x] 清理 console.log/error/warn（40处全部 DEV 守卫化）
- [x] **v0.9.0 前端类型断言清零（2026-09-20）**:
  - [x] `as unknown as` 双重断言全量治理（61 → 0，39 文件；剩余 2 处为 unwrap.ts 工具注释）
  - [x] 替换策略: API 解包→unwrapData/unwrapPaginated；alova HTTP→泛型；组件 props→defineProps 泛型；Matrix SDK→类型声明

## 3. 待办事项

### 3.1 高优先级

- [ ] **路由治理**：清理未在路由中注册的 admin 页面文件
- [ ] **admin/tenant 去重**：两端页面高度重复，参考 sesms 三端分工模式重构
- [ ] **console 清理验证**：确认所有 DEV 守卫在生产构建中被正确剔除

### 3.2 中优先级

- [ ] **API 层统一**：将 admin 内联 API 移到共享 api 包
- [ ] **依赖升级**：Quasar / Vite / Vue 版本对齐

### 3.3 低优先级

- [ ] **Bundle 分析**：优化首屏加载

## 4. 构建脚本

```bash
# 开发
cd Frontend
pnpm --filter myai-admin dev      # http://localhost:9000
pnpm --filter myai-tenant dev     # http://localhost:9001
pnpm --filter myai-ops dev        # http://localhost:9002
pnpm --filter myai-social dev     # http://localhost:9003

# 构建
pnpm --filter myai-admin build
pnpm --filter myai-tenant build
pnpm --filter myai-ops build
pnpm --filter myai-social build

# 类型检查
cd Frontend/apps/admin
npx vue-tsc --noEmit
```

## 5. 代码统计

| 维度                | 数量           |
| ------------------- | -------------- |
| Vue 文件（apps） | 67 |
| Vue+TS 文件（packages） | 242 |
| TypeScript 文件（全） | 538 |
| 包共享 | 10 个 packages |
| 应用 | 4 个 |
| admin 页面 | 47 |
| packages/pages | 111 |
| packages/components | 171 |
| packages/composables | 56 |
| packages/types | 23 |
| packages/utils | 17 |
