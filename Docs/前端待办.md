# erp_new 前端待办清单

> 基于 Vue 3 + Quasar 的全栈微服务管理平台前端

## 1. 项目现状

### 1.1 应用结构

| 应用 | 框架 | 页面数 | 说明 |
|---|---|---|---|
| admin | Vue3 + Quasar | 47 | 平台运营（租户/计费/目录/票据/系统） |
| tenant | Vue3 + Quasar | - | 单位业务（路由引用 packages/pages） |
| ops | Vue3 + Quasar | - | 运营面板（路由引用 packages/pages） |
| social | Vue3 + Quasar | - | 社交端（路由引用 packages/pages） |

### 1.2 共享层（packages/）

| 包 | 说明 |
|---|---|
| api | API 封装（alova） |
| boot | 启动引导（alova 实例 + 认证/租户注入） |
| capabilities | 能力开关 |
| components | 共享组件 |
| composables | 组合式函数 |
| i18n | 国际化 |
| pages | 共享页面（111 vue，103 导出） |
| stores | 共享状态 |
| types | 类型定义 |
| utils | 工具函数 |

## 2. 已完成 ✅

- [x] 清理 `node_modules/`、`dist/`、`.turbo/`、`.quasar/`、`.kilo/` 缓存
- [x] 更新 `.gitignore`（.turbo/、.quasar/、.kilo/、.vscode/）
- [x] 添加 `deploy/docker-compose.yml`
- [x] 重构 `packages/pages/src/index.ts`（103 真实导出）
- [x] 删除 `apps/tenant/src/pages/`（92 未引用文件）
- [x] 删除 `apps/ops/src/pages/`（19 未引用文件）
- [x] 删除 `apps/social/src/pages/`（13 未引用文件）
- [x] 删除 admin 9 个薄封装页面 + 清理路由
- [x] 清理 TypeScript `any` 类型（11 → 0）

## 3. 待办事项

### 3.1 高优先级

- [ ] **路由治理**：清理未在路由中注册的 admin 页面文件
- [ ] **admin/tenant 去重**：两端页面高度重复，参考 sesms 三端分工模式重构

### 3.2 中优先级

- [ ] **API 层统一**：将 admin 内联 API 移到共享 api 包
- [ ] **依赖升级**：Quasar / Vite / Vue 版本对齐

### 3.3 低优先级

- [ ] **E2E 测试**：Playwright / Cypress
- [ ] **Lighthouse 审计**：性能、可访问性
- [ ] **Bundle 分析**：优化首屏加载

## 4. 构建脚本

```bash
# 开发
cd Frontend
pnpm --filter myai-admin dev

# 构建
pnpm --filter myai-admin build

# 类型检查
cd Frontend/apps/admin
npx vue-tsc --noEmit
```

## 5. 代码统计

| 维度 | 数量 |
|---|---|
| Vue 文件 | 199 |
| TypeScript 文件 | 554 |
| SCSS 文件 | ~20 |
| 包共享 | 10 个 packages |
| 应用 | 4 个 |
| 总文件 | ~770 |
