# =============================================================================
# MyAI Admin 前端 - 开发环境变量 (quasar dev / vite)
# =============================================================================
# 优先级: .env.development > .env (仅 dev 模式加载)
# 本地开发默认后端: http://localhost:8090 (api-gateway)
# =============================================================================

VITE_API_BASE_URL=http://localhost:8090/api
VITE_WS_URL=ws://localhost:8090/ws
VITE_APP_TITLE=MyAI Admin (Dev)